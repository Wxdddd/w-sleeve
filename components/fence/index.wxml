<view class="container">
    <view class="title">{{fence.title}}</view>
    <view class="row-container">
        <block wx:for="{{fence.cells}}" wx:key="{{index}}">
            <w-cell y="{{index}}" x="{{x}}" class="cell" cell="{{item}}"></w-cell>
        </block>
        <view class="hr"></view>
    </view>
</view>
